version: "3.2"
services:

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    image: "ddmal/nginx:fix_dev"

  iipsrv:
    build:
      context: ./iipsrv
      dockerfile: Dockerfile
    image: "ddmal/iipsrv:nightly"

  rodan:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BRANCHES: develop
    image: "ddmal/rodan:fix_dev"

  py3-celery:
    build:
      context: .
      dockerfile: ./python3-celery/Dockerfile
      args:
        BRANCHES: develop
    image: "ddmal/rodan-python3-celery:fix_dev"

  gpu-celery:
    build:
      context: .
      dockerfile: ./gpu-celery/Dockerfile
      args:
        BRANCHES: develop
    image: "ddmal/rodan-gpu-celery:fix_dev"

  postgres:
    build:
      context: ./postgres
      dockerfile: Dockerfile
    image: "ddmal/postgres-plpython:fix_dev"

  rodan-client:
    build:
      context: ./rodan-client
      dockerfile: Dockerfile
    image: "ddmal/rodan-client:fix_dev"

  hpc-rabbitmq:
    build:
      context: ./hpc-rabbitmq
      dockerfile: Dockerfile
    image: "ddmal/hpc-rabbitmq:fix_dev"
