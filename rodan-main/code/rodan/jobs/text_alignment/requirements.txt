# text_alignment OCR runs on upstream Calamari-OCR, which supports Python 3.11 and
# TensorFlow <= 2.15 via ocrd-fork-tfaip 1.2.7 (replaces the old DDMAL calamari/tfaip
# forks that were cp37 / TF 2.5.1 only). TensorFlow itself is provided by the GPU image
# (tensorflow[and-cuda]==2.15.1); calamari pulls scipy/paiargparse/pillow transitively.
# The bundled salzinnes_stgall model has been pre-migrated to the calamari v6 SavedModel
# format so it loads without any runtime migration write.
calamari-ocr==2.3.1
numpy==1.26.4
scikit-image>=0.22.0
